.row
  .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
    - @posts.each do |post|
      .post-preview
        = link_to post do
          %h2.post-title
            = post.title.titleize
          %h3.post-subtitle
            = truncate(post.body, length: 100, omission: "... Read More")
        %p.post-meta
          Posted by
          %a{:href => "#"} bunglify
          on
          = post.created_at.strftime("%e")
      %hr/
    / Pager
    = paginate @posts
